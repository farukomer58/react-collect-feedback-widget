/**
 * Constants used throughout the feedback widget.
 * Centralized location for magic numbers and default values.
 */

/** Default z-index for the modal overlay */
export const DEFAULT_MODAL_Z_INDEX = 50;

/** Default z-index for the feedback icon */
export const DEFAULT_ICON_Z_INDEX = 30;

/** Z-index offset between icon and modal */
export const Z_INDEX_OFFSET = 20;

/** Auto-close delay in milliseconds after successful submission */
export const SUCCESS_AUTO_CLOSE_DELAY = 2000;

/** Maximum star rating value */
export const MAX_RATING = 5;

/** Minimum star rating value */
export const MIN_RATING = 1;

/** Default primary color */
export const DEFAULT_PRIMARY_COLOR = '#3b82f6';

/** Default error background color */
export const DEFAULT_ERROR_BG_COLOR = '#fee2e2';

/** Default error text color */
export const DEFAULT_ERROR_TEXT_COLOR = '#dc2626';

